spring.application.name=pks

# Spring Datasource (MySQL) - set these values for your local environment
spring.datasource.url=jdbc:mysql://localhost:3306/incometaxdb?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Pks@12345

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server port (optional)
server.port=8080
